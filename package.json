{
  "name": "projectx-mcp",
  "version": "1.0.0",
  "description": "MCP server for ProjectX time tracker automation",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/server.ts",
    "start": "node dist/server.js",
    "save-session": "ts-node scripts/save-session.ts",
    "test:entry": "ts-node scripts/test-entry.ts",
    "build:installer": "bash scripts/build-pkg.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "playwright": "^1.49.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
